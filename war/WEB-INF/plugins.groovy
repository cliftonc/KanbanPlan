/*
 * Gaelyk Plugins Registration Configuration File
 * 
 * List each plugin descriptor file name here.
 * 
 * Plugin descriptors file are the groovy scripts
 * added to war/WEB-INF/plugins.
 * 
 * Do not include the plugin descriptor file's file type (ie. .groovy),
 * just include the plugin descriptor file's file name.
 * 
 * Example: If in war/WEB-INF/plugins is a plugin descriptor file whose name
 * is plugindescriptor.groovy you would add the following entry to this file 
 *  
 *  'install plugindescriptor'
 */

install objectifyplugindescriptor	// objectify support